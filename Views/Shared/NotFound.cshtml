@model string
@{
    ViewBag.Title = "Sayfa Bulunamadı";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<div class="container mt-5">
    <div class="row justify-content-center">
        <div class="col-md-6 text-center">
            <div class="error-page">
                <i class="fas fa-search text-info" style="font-size: 4rem;"></i>
                <h2 class="mt-3">404 - Sayfa Bulunamadı</h2>
                <p class="text-muted">@(Model ?? "Aradığınız sayfa bulunamadı. Sayfa kaldırılmış, taşınmış veya geçici olarak kullanılamıyor olabilir.")</p>
                <div class="mt-4">
                    <a href="@Url.Action("Index", "Home")" class="btn btn-primary">
                        <i class="fas fa-home me-2"></i>Ana Sayfaya Dön
                    </a>
                    <button onclick="history.back()" class="btn btn-secondary">
                        <i class="fas fa-arrow-left me-2"></i>Geri
                    </button>
                </div>
            </div>
        </div>
    </div>
</div>

<style>
.error-page {
    padding: 2rem;
    border-radius: 10px;
    background: #f8f9fa;
    border: 1px solid #dee2e6;
}
</style>